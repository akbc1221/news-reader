import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
  list: {
    width: 180,
    paddingLeft: 10,
    paddingRight: 5,
  },
  fullList: {
    width: "auto",
  },
  item: {
    height: 60,
    borderRadius: 3,
  },
});

export default useStyles;
